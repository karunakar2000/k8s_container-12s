- name: gather facts
  hosts: local
  connection: local
  # ansible collect the info of target server, before it executes any other fact.
  tasks:
  - name: facts
    ansible.builtin.debug:
      msg: "{{ ansible_facts }}"
